<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cds_app_part1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CDS_App_Part1">
        <activity android:name=".Employee_Login_Activity"></activity>
        <activity android:name=".Recruiter_Login" />
        <activity android:name=".Recruiter_FirstTime" />
        <activity
            android:name=".ui.login.Recruiter_Login"
            android:label="@string/title_activity_recruiter_login" />
        <activity android:name=".MailAppOpensActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>